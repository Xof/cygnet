# Integration tests require a live PostgreSQL instance.
# Run with:  pytest tests/integration -m integration
# Set env:   CYGNET_TEST_DSN=postgresql://user:pass@localhost/cygnet_test
# Or use:    just test-all   (spins up Docker automatically)
